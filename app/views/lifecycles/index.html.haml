%h1 Listing lifecycles

%table
  %tr
    %th Name
    %th Description
    %th
    %th
    %th

  - @lifecycles.each do |lifecycle|
    %tr
      %td= lifecycle.name
      %td= lifecycle.description
      %td= link_to 'Show', lifecycle
      %td= link_to 'Edit', edit_lifecycle_path(lifecycle)
      %td= link_to 'Destroy', lifecycle, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Lifecycle', new_lifecycle_path
